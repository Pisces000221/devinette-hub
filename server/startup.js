Meteor.startup(function () {
  return Meteor.methods({
/* TODO: add methods here
 *  add<Data>: function (<data, like 'num' or 'party_id'>) {
 *    if (Meteor.userId() === null) return null;
 *    check(data, Number);
 *    var id = Random.id();
 *    <Database>.insert({
 *      _id: id,
 *      owner: Meteor.userId(),
 *      timestamp: (new Date()).getTime(),
 *      data: <data>
 *    });
 *    return id;
 *  } */
  });
});
